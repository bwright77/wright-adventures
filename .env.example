# ============================================================
# Wright Adventures OMP — Local environment template
# Copy to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# ============================================================

# ── Supabase (frontend) ──────────────────────────────────────
# Dashboard: https://supabase.com/dashboard/project/<ref>/settings/api
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ── Supabase (server-side — API functions only) ──────────────
# Same URL as above, no VITE_ prefix. Service role key bypasses RLS.
# Dashboard: Settings → API → service_role key (keep secret)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ── Anthropic ────────────────────────────────────────────────
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# ── Simpler.Grants.gov (Phase 3 — ADR-002) ───────────────────
# Register at: https://simpler.grants.gov/developer
# Used by /api/discovery/sync to search and fetch federal grant opportunities.
SIMPLER_GRANTS_API_KEY=your-simpler-grants-api-key-here

# ── Cron secret (Phase 3 — ADR-002) ─────────────────────────
# Secures the /api/discovery/sync endpoint against unauthorized triggers.
# Generate: openssl rand -hex 32
# Must match the value set in Vercel dashboard for the cron job to work.
# Leave blank locally if you are triggering sync manually via the admin UI.
CRON_SECRET=
